# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'firebaseImageUpload' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for firebaseImageUpload

  pod 'Firebase/Core'
  pod 'Firebase/Storage'
  
  target 'firebaseImageUploadTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'firebaseImageUploadUITests' do
    # Pods for testing
  end

end
